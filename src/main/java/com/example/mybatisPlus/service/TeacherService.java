package com.example.mybatisPlus.service;

import org.springframework.stereotype.Service;

@Service(value = "teacherService")
public class TeacherService {

   /* @Autowired
    TeacherMapper teacherMapper;*/

 /*   public Teacher selectOne(Teacher teacher){
       return teacherMapper.selectOne(teacher);
    }

    public List<Teacher>  selectPage(Page<Teacher> page, EntityWrapper<Teacher> entityWrapper){
        return teacherMapper.selectPage(page,entityWrapper);
    }

    public void insert(Teacher teacher) {
        teacherMapper.insert(teacher);
    }

    public void delete(EntityWrapper entityWrapper) {
        teacherMapper.delete(entityWrapper);
    }

    public void update(Teacher teacher, EntityWrapper entityWrapper) {
        teacherMapper.update(teacher,entityWrapper);
    }

    public List<Teacher> selectByMap(Map<String, Object> hashMap) {
        return teacherMapper.selectByMap(hashMap);
    }

    public int selectCount(EntityWrapper<Teacher> entityWrapper) {
        return teacherMapper.selectCount(entityWrapper);
    }

    public List<Teacher> selectBatchIds(List<Integer> idList) {
        return teacherMapper.selectBatchIds(idList);
    }

    public List<Teacher> selectList(EntityWrapper entity) {
        return teacherMapper.selectList(entity);
    }*/
}